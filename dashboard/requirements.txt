# Intentionally empty.
#
# The dashboard uses ONLY the Python 3.10+ standard library — no third-party packages,
# no build step, nothing to pip install. This file exists so that the answer to "are
# there dependencies?" is written down rather than inferred from its absence.
#
# Runs under any python3.
